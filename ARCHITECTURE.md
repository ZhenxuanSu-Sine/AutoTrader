# AutoTrader 架构与路线

## 设计目标

框架需要同时服务三类工作负载：低频策略快速验证、分钟/小时级组合回测，以及深度学习和
因子挖掘的数据生产。三者共享数据和评测口径，但不应共享供应商细节或特定回测库对象。

核心原则：

1. **时间可得性优先**：每份数据都必须能回答“在什么时间点可知”，成交默认滞后一根 K 线。
2. **研究与执行分离**：策略只输出目标仓位；撮合、费用和交易制度由执行层负责。
3. **长表作为交换格式**：日线、分钟线和多资产使用同一接口；宽表只作为计算时的临时视图。
4. **可复现实验**：数据版本、参数、代码版本和随机种子最终都进入实验清单。
5. **供应商可替换**：AKShare 可以承担原型验证，但任何供应商字段都不能进入策略层。

## 当前数据流

```text
供应商 DataFrame
       │
       ▼
normalize_bars / validate_bars
       │  统一长表 + 明确失败
       ├──────────────► factors ──► IC / 分位收益
       │
       ▼
strategy ──► target_weights
                    │
                    ▼（下一根 bar 开盘）
             PortfolioEngine
                    │
          ┌─────────┼──────────┐
          ▼         ▼          ▼
        trades   positions    equity ──► metrics
```

## 模块边界

### 数据层

`autotrader.data` 定义唯一行情契约。供应商适配器负责映射、单位换算和原始响应留档；存储层
负责分区、版本和增量更新；策略不感知数据来自 AKShare、付费接口还是本地文件。

当前 bar 主键为 `(timestamp, symbol)`。未来的正式数据湖建议按
`asset_type/frequency/trading_date/symbol` 分区存储 Parquet，并将证券主数据、交易日历、
复权因子和财务数据拆成独立的 point-in-time 表。

### 策略层

策略输入标准行情/特征，输出三列：`timestamp, symbol, weight`。权重在该时间点收盘后可知，
回测器在下一根 bar 执行。这个约定让规则策略、树模型和深度学习模型使用同一执行接口。

### 回测层

当前 `PortfolioEngine` 是确定性的逐 bar 组合模拟器，支持多标的、现金约束、整手买入、T+1
和非对称交易成本。它适合研究筛选和中低频组合策略。

尚未实现的市场微观结构包括涨跌停封单、停牌、集合竞价、成交量参与率、部分成交、公司
行动及不同品类的结算规则。这些能力应通过可替换的 `Broker/ExecutionModel` 加入，而不是
塞进策略代码。

`autotrader.evaluation.rolling` 在该引擎之上编排自然月滚动窗口。它不复制撮合逻辑：每窗重新
初始化引擎、切取因果信号、末日清仓，然后将逐窗结果聚合为分布统计。大于一个月的窗口默认
每月滚动，因此相邻样本会重叠；这些窗口适合观察起止时点敏感性，但不能当作相互独立样本做
显著性检验。

### 因子层

因子输出 `(timestamp, symbol, factor)`，评测层按证券计算未来收益，按时间做横截面 Rank IC
和分位数组合。后续需增加行业/市值中性化、去极值、标准化、换手和容量分析，并对财务因子
使用公告时间而非报告期时间。

## 分阶段计划

### 阶段 1：可信研究内核（当前）

- 统一行情契约和质量校验
- 无未来函数的目标仓位回测
- A 股基础费用、整手和 T+1
- 基础绩效与因子评测
- 离线示例和回归测试

### 阶段 2：数据底座

- 定义 `DataSource`、`BarStore`、`TradingCalendar` 接口
- 接入一个稳定的日线源和一个分钟线源
- 建证券主表，处理代码、上市/退市、ST、停牌和复权
- Parquet 分区、增量同步、校验报告和数据快照版本
- CLI：`ingest`、`validate`、`catalog`

### 阶段 3：组合回测真实性

- 涨跌停、停牌、成交量约束、部分成交
- 分红送转和退市处理
- 基准、行业和风险暴露
- 订单级成交归因、容量与成本敏感性
- walk-forward 和滚动训练/验证/测试切分

### 阶段 4：因子与机器学习研究

- point-in-time 特征库与因子注册表
- 截面预处理、正交化、中性化和组合构建
- Purged/embargo 时序交叉验证
- 数据集窗口化、标签生成、模型预测缓存
- MLflow 或等价实验追踪；模型与数据版本绑定

### 阶段 5：仿真和实盘

- 事件驱动 broker 接口、订单状态机和持仓对账
- 模拟盘适配器、风控闸门、幂等重试和审计日志
- 实盘与回测共享策略接口，但使用独立执行实现
- 监控数据延迟、信号漂移、成交偏差和风险暴露

## 近期优先级

下一步应优先完成阶段 2，而不是急着训练深度网络。分钟数据量大不等于有效样本多；如果证券
主数据、复权、停牌和时间可得性没有先固定，模型很容易学到幸存者偏差或未来信息。数据层
稳定之后，再选择具体模型和训练切分会更划算。
