# AutoTrader TODO

## 数据与股票池

- 接入历史指数成分股，替换当前 CSI300 Top40 静态快照，降低幸存者偏差。
- 增加 ST、停牌、上市天数、涨跌停、退市过滤。
- 增加历史行业分类，支持行业中性化和行业暴露约束。
- 扩展到更大的 A 股 universe；当前 40 只股票对机器学习和因子挖掘都偏小。
- 稳定接入分钟级数据，用于短线策略、日内信号和后续深度学习。

## 回测与风控

- 增加组合级动态风控：
  - 回撤接近阈值时降仓；
  - 触及最大回撤预算时强制空仓；
  - 创新高后逐步恢复仓位。
- 增加单日亏损限制、单票集中度限制、行业集中度限制。
- 当前负现金模型按固定年化利率计息，后续需要模拟融资额度、维持担保比例、追保和平仓线。
- 如果接入股指期货，需要增加合约乘数、保证金、逐日盯市、换月、基差和到期处理。

## 策略研究

- 当前较好的进攻候选：
  - `aggressive_breakout_top3_g2`：约 28.8% 年化、1.06 Sharpe、-29.5% 最大回撤。
  - `ml_hgb_h5_top3_weekly`：约 32.2% 年化、1.18 Sharpe、-34.3% 最大回撤。
- 当前较好的风控候选：
  - `ml_hgb_h5_top3_risk0.40`：约 12.7% 年化、1.19 Sharpe、-14.6% 最大回撤。
- 短线稀疏趋势策略能抓少数右尾月份，但 Sharpe 和回撤暂时不理想。
- Sharpe 2 搜索已尝试：
  - HGB 市场状态过滤；
  - 预测分数阈值；
  - HGB TopN / horizon / rebalance 变体；
  - 单资产规则搜索；
  - 策略组合；
  - 权益曲线择时。
  当前没有找到可信 Sharpe 2 候选，详见 `SHARPE2_SEARCH_RESULTS.md`。

## 机器学习

- 给 ML 模型增加 walk-forward 诊断：
  - 每轮训练样本数；
  - 特征重要性；
  - 预测分布；
  - 分数校准；
  - 分时段表现。
- 增加 purged / embargo cross-validation，避免标签重叠造成过高估计。
- 扩展 LightGBM / XGBoost 参数搜索；当前 CPU 可以跑，但需要控制时间。
- 将 ML alpha 与独立风险模型拆分，避免模型同时承担预测和风控。

## 深度学习备案

暂不在当前纯 CPU 项目里直接引入深度学习训练。启动前需要先完成：

- 历史成分股或全 A 股票池；
- 稳定分钟级数据；
- 严格训练 / 验证 / 测试时间切分；
- GPU 环境；
- 实验追踪：数据版本、特征版本、模型参数、随机种子、训练时间。

候选方向：

- Temporal CNN / TCN；
- 轻量 Transformer Encoder；
- TabNet / FT-Transformer；
- 多任务模型：同时预测未来收益、波动率、下行风险；
- 深度组合模型：直接输出受约束目标权重。

深度学习模型必须和 LightGBM / XGBoost 强 baseline 对比，只有显著超过树模型时才值得继续。
